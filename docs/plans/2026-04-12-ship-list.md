# ACE Desktop — Ship List (2026-04-12)

> Today's ordered execution list. Joe Hawley installs tomorrow (Apr 13).
> Parent docs: [ROADMAP.md](../../ROADMAP.md) · [Sprint Plan](2026-04-12-ace-desktop-sprint-plan.md)
> Hard gate: **21:30 Max credits cutoff (DOWNGRADE MAX)**

---

## Bugs + readability first (Joe sees these immediately)

1. [x] **Zoom bug — chat composer clipped**
   When user zooms (Cmd +/-), the bottom chat input gets cut off. Likely a viewport-height or flex container issue. Reproduce in DevTools, fix layout so composer always stays visible regardless of zoom level.
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *High priority*

2. [x] **Dark mode font legibility**
   Shipped: `--text-dim #606080→#8c8ca8`, `--gold-dim #7a60b0→#a080c8` ([tokens.css](../../renderer/styles/tokens.css)). Zoom range 75–150% → 50–200% ([theme.js:40](../../renderer/modules/theme.js#L40)).
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11)

3. [x] **Chat text too white**
   Already addressed: chat body uses `--text-primary: #d8d4e4` (not `#fff`). Verified on live app. No change needed.
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11)

---

## Quick wins

4. [x] **Somatic warmth — token glow fix**
   Shipped: killed the full-pane amber/red radial wash on `.term-pane::before`. Pressure now carried by the ctx-bar (gold breath → red breath → red pulse) and the header dot (color-matched + radial ring ping). Reading surface stays clean. Files: [terminal.css](../../renderer/styles/views/terminal.css), [chat.css](../../renderer/styles/chat.css). Backlog: session-state-driven header dot (active/idle/stale/error).
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11)

5. [x] **Terminal session naming**
   Shipped: new `deriveSessionName()` in [session-manager.js](../../renderer/modules/session-manager.js) auto-names each session from its first user prompt (trim + collapse whitespace + 28-char truncate with `…`). Tab label updates on first send; header label stays static "ACE SESSION" as a category marker (tab = identity, header = category). Bonus fix: tab bar is now responsive — tabs flex-shrink with `min-width: 50px` floor, horizontal scroll fallback, fixed-size dot/close button survive the squeeze. Files: [session-manager.js](../../renderer/modules/session-manager.js), [terminal.css](../../renderer/styles/views/terminal.css).
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11)

6. **Notification system redesign**
   Replace red dot with amber/accent. Click notification → jump to correct session pane with pulsing dot on tab. Multiple notifications → dropdown to pick which chat.
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *Medium priority*

---

## Medium features

7. **Dynamic command registry**
   `command-registry.js` reads `.claude/skills/*/SKILL.md` from vault at runtime via existing `vault.listDir` + `vault.readFile` IPC. Merges with static COMMANDS. Both Cmd+K and slash menu auto-discover custom skills. ~30 lines, no new IPC.
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *High priority*

8. **Scratchpad**
   Persistent markdown notepad sidebar.
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *Medium priority*

9. **Living orb**
   Animated orb reacting to PTY activity / session state.
   → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *Medium priority*

10. **Token Economy Phase 2 — context lifecycle**
    Auto-effort at 80%, brevity injection at 70%, cache countdown + refresh button.
    → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *High priority* · [token-economy-overhaul.md](../../ace-desktop-token-economy-overhaul.md)

---

## Heavier (do only if time)

11. **Self-healing renderer refresh**
    (1) `bootedAt` timestamp in `state.js`, (2) refresh-window config (idle >30min + uptime >12h, or 2am–6am quiet hours), (3) state audit — chat sessions graceful teardown, PTY cleanup, atmosphere counters already persist, (4) IPC channel `RENDERER_REFRESH`, (5) post-reload hydration. Half-day.
    → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *Medium priority*

12. **Auto-sync health system**
    Start with Artifacts only. Extend to People/Dashboard later. "Last synced" indicator per view. Stale-data health flag integrates with system integrity bar.
    → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *High priority*

---

## After all features are in — schema is now frozen

13. **Update ROADMAP.md**
    Mark all shipped rows → Done (live update, not batch).
    → [ROADMAP.md](../../ROADMAP.md)

14. **DCA frontmatter integration** *(fresh chat — per plan note)*
    - Read `parseDCAFrontmatter()` in `src/vault-reader.js` → lock schema
    - Update `ace-core/00-System/core/dca.md` template with empty scaffold matching schema
    - Extend `/build-vault` skill to populate from Blueprint (anchors, gate_date, journey_start, affirmations, compass_directions)
    → [ROADMAP.md](../../ROADMAP.md#in-progress--now-apr-11) · *High priority* · [DCA schema memory](../../../.claude/projects/-Users-nikhilkale-Documents-Actualize/memory/reference_dca_frontmatter_schema.md)

15. **Onboarding tutorial**
    Post-setup guided walkthrough shown once. Introduces the Triad, explains daily rituals (`/start`, `/eod`), tours the dashboard widgets and views, shows where to get help. Progressive disclosure.
    → [ROADMAP.md](../../ROADMAP.md#in-progress--phase-1-mac-ship-apr-12-13) · *Phase 1 blocker*

---

## Ship prep

16. **Unsigned DMG build (Path B)**
    Skip Apple Dev ID enrollment today. Build unsigned DMG via `npm run dist` without signing config.
    → [ROADMAP.md](../../ROADMAP.md#in-progress--phase-1-mac-ship-apr-12-13) · *Code signing / notarization row*

17. **Build + verify DMG locally**
    Install on your Mac as a fresh user would. Confirm cockpit populates (DCA frontmatter), no crash, onboarding flows.

18. **Write Joe's install note**
    3 lines for Joe:
    > macOS will block this on first open since I don't have Apple Dev ID yet (coming this week). Right-click the app → Open → Open anyway. One-time. Then it launches normally.

19. **/close before 21:30**
    Capture what shipped, what slipped, update ROADMAP.

---

## Tomorrow (Apr 13)

- First deploy test → Joe Hawley macOS build
- Live fix any install-blocking issues
- Apple Dev ID enrollment in parallel this week → signed auto-updating build to Joe within a week

---

## Ruthless scope cut if time gets tight

- **Must ship:** 1–6 (bugs + quick wins), 13–18 (ROADMAP + DCA + tutorial + DMG + note)
- **Try to ship:** 7–10 (medium features)
- **Drop if running long:** 11–12 (heavier)
- **Non-negotiable:** 17 (verify DMG locally), 19 (/close)

---

## Status Legend

Mark inline as you ship:
- `[ ]` Not started
- `[~]` In progress
- `[x]` Shipped
- `[!]` Blocked / deferred
